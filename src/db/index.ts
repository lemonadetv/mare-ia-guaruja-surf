import{neon}from"@neondatabase/serverless";import{drizzle}from"drizzle-orm/neon-http";import*as schema from"./schema";let cached:ReturnType<typeof create>|null=null;function create(){if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL não configurada");return drizzle(neon(process.env.DATABASE_URL),{schema})}export function getDb(){return cached??(cached=create())}
