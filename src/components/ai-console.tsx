"use client";import{useState}from"react";import{MessageCircle,Send,Sparkles}from"lucide-react";const suggestions=["Tombo ou Astúrias?","Melhor horário cedo","Fish ou shortboard?"];
export function AIConsole(){const[question,setQuestion]=useState("");const[answer,setAnswer]=useState("Escolha uma pergunta rápida ou escreva do seu jeito. Eu respondo usando a previsão disponível.");const ask=(value=question)=>{if(!value.trim())return;setAnswer("Hoje, Tombo aparece como a opção mais consistente. Confira a janela horária antes de sair: vento e período podem mudar o ranking.");setQuestion("")};return <div className="ai-wrap"><div className="ai-console panel"><header className="ai-head"><span className="icon-orb"><Sparkles size={19}/></span><div><small>IA DO MUNRRAT</small><strong>Resposta local e direta</strong></div><span className="online">ONLINE</span></header><div className="ai-message"><MessageCircle size={19}/><p>{answer}</p></div><div className="ai-suggestions">{suggestions.map(x=><button key={x} onClick={()=>ask(x)}>{x}</button>)}</div><div className="ai-form"><input value={question} onChange={e=>setQuestion(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")ask()}} aria-label="Pergunta para a IA do Munrrat" placeholder="Pergunte sobre praia, horário ou prancha"/><button onClick={()=>ask()} aria-label="Enviar pergunta"><Send size={17}/></button></div></div></div>}
