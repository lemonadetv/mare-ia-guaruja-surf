"use client";
import {useState} from "react";import {MessageCircle,Send,Sparkles} from "lucide-react";
export function AIConsole(){const [question,setQuestion]=useState("");const [answer,setAnswer]=useState("Pergunte sobre horários, picos ou pranchas. Eu cruzo sua pergunta com o motor de previsão.");const ask=()=>{if(!question.trim())return;setAnswer("Hoje eu começaria olhando Guaiúba e Tombo. A decisão final depende do horário escolhido na régua — não vou inventar valores fora da previsão disponível.");setQuestion("")};return <div className="ai-console panel"><div className="ai-head"><span className="icon-orb"><Sparkles/></span><div><small>IA DO MUNRRAT</small><strong>Leitura local, resposta direta.</strong></div><span className="online">● ONLINE</span></div><div className="ai-message"><MessageCircle size={20}/><p>{answer}</p></div><div className="ai-form"><input value={question} onChange={e=>setQuestion(e.target.value)} onKeyDown={e=>e.key==="Enter"&&ask()} aria-label="Pergunta para a IA do Munrrat" placeholder="Tombo ou Astúrias amanhã cedo?"/><button onClick={ask} aria-label="Enviar pergunta"><Send size={18}/></button></div></div>}
