import{NextResponse}from"next/server";import{getForecast}from"@/lib/providers";export async function GET(){const started=Date.now();const data=await getForecast();return NextResponse.json({status:data.available?"operacional":"degradado",latencyMs:Date.now()-started,checkedAt:new Date().toISOString(),providers:{...data.providers,stormglass:process.env.STORMGLASS_API_KEY?"configurado":"não configurado",cetesb:"aguardando fonte oficial estruturada",googleMaps:process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY?"configurado":"não configurado",ai:process.env.OPENAI_API_KEY?"configurado":"fallback determinístico"}})}
