import{spots}from"@/lib/spots";export default function Map(){return <main><p className="eyebrow">MAPA DO SURF</p><h1 style={{fontSize:48}}>Guarujá, pico a pico.</h1><div className="panel" style={{minHeight:520,padding:30,background:"radial-gradient(circle at 60% 50%,#174a48,#0b1c1c 55%,#081111)"}}><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><span className="pill good">✓ PICOS</span><span className="pill">○ SWELL</span><span className="pill">○ VENTO</span></div><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",marginTop:60}}>{spots.map((s,i)=><div className="panel" key={s.id} style={{padding:16,transform:`translateY(${(i%3)*28}px)`}}><strong>{s.name}</strong><p className="muted">{s.lat.toFixed(3)}, {s.lon.toFixed(3)}</p></div>)}</div><p className="muted" style={{marginTop:80}}>O mapa vetorial do Google é ativado quando a chave autorizada é provisionada. Os picos e coordenadas permanecem disponíveis sem dados fictícios.</p></div></main>}
